name: Build & Release APK

# Runs every time AI Studio (or you) pushes to main.
# Also runnable manually from the Actions tab.
on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

# Needed so the workflow is allowed to create a Release.
permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 9.3.1

      - name: Generate debug keystore
        run: keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

      - name: Build debug APK
        run: gradle assembleDebug --stacktrace

      - name: Set build tag
        id: vars
        run: echo "tag=build-${{ github.run_number }}" >> "$GITHUB_OUTPUT"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.vars.outputs.tag }}
          name: "সমাগত খেরোখাতা – build ${{ github.run_number }}"
          make_latest: true
          # If your AI Studio project's module isn't named "app",
          # update this path to match (check settings.gradle).
          files: app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# ---------------------------------------------------------------
# UPGRADING TO A SIGNED RELEASE BUILD (optional, do this later)
# ---------------------------------------------------------------
# 1. Generate a keystore once, locally:
#      keytool -genkeypair -v -keystore release-key.jks -alias attendance \
#        -keyalg RSA -keysize 2048 -validity 10000
# 2. Base64-encode it and save as a GitHub secret named KEYSTORE_BASE64:
#      base64 -w0 release-key.jks   (Linux/Mac)
#      certutil -encode release-key.jks tmp.b64   (Windows)
# 3. Add three more secrets: KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
# 4. Replace the "Build debug APK" step with:
#
#      - name: Build release APK (unsigned)
#        run: ./gradlew assembleRelease --stacktrace
#
#      - name: Sign APK
#        id: sign_apk
#        uses: r0adkll/sign-android-release@v1
#        with:
#          releaseDirectory: app/build/outputs/apk/release
#          signingKeyBase64: ${{ secrets.KEYSTORE_BASE64 }}
#          alias: ${{ secrets.KEY_ALIAS }}
#          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
#          keyPassword: ${{ secrets.KEY_PASSWORD }}
#
#    ...and change the release "files:" line to:
#          files: ${{ steps.sign_apk.outputs.signedReleaseFile }}
# ---------------------------------------------------------------
